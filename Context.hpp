#ifndef CONTEXT_HPP
#define CONTEXT_HPP
#include "Command.hpp"
/*

class Context
{
private:
	vector<Command *> commands;
public:
	Context();
	~Context();
	void load(vector<Command *>& commands);
	void unload(vector<Command *>& commands);
};
*/

#endif
